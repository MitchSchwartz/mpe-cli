name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  shell-tests:
    name: shell tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: tests/test_snapshot.sh
        run: bash tests/test_snapshot.sh
